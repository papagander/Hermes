
This project defines the entities used to create + persist report queries in a database.
Currently, it uses a sqlite file to mock an actual database. 
A migration of the code-first database can be obtained by using the command "add-migration"
on the 
The HermesSeeder project is used to 